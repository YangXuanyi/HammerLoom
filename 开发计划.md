# 第一阶段开发计划：Context 与 Skill 进化

## 1. 算法灵感和创新点

### 1.1 灵感来自哪里

第一阶段主要参考 2026 年论文 **Escaping the Self-Confirmation Trap: An Execute-Distill-Verify Paradigm for Agentic Experience Learning**（EDV，arXiv:2606.24428）。

现有 Agent 通常这样积累经验：

1. Agent 执行任务；
2. 从成功轨迹中总结一段经验；
3. 将经验写入记忆库；
4. 遇到相似任务时检索并使用这段经验。

问题在于，一次任务成功不代表总结出的经验是正确的。成功可能来自偶然的工具状态、额外尝试或其他上下文。Agent 也可能对自己的错误结论给出看似合理的解释，然后把错误经验反复用于后续任务。

EDV 的改进是将过程拆成三步：多个 Agent 执行任务，另一个 Agent 从成功和失败轨迹中提炼经验，再由执行任务的 Agent 投票决定经验是否可以写入记忆。它给我们的核心启发是：

> 轨迹产生的经验不能直接使用，必须先作为候选进行验证。

但 EDV 的验证仍然依赖 Agent 判断。一条经验即使得到多个 Agent 一致认可，也不代表它能真正提高后续任务的成功率。多个 Agent 可能犯同一种错误，而且这种投票无法发现经验对旧任务的破坏、在不同任务上的误用以及额外成本。

### 1.2 我们做了哪些改进

#### 改进一：用实际运行结果验证经验

我们不询问 Agent“这条经验是否正确”，而是分别运行两次同一个任务：

- 第一次不使用候选 Skill；
- 第二次使用候选 Skill；
- 比较两次的任务结果、token、耗时和安全事件。

只有使用 Skill 后结果确实变好，才能证明这条 Skill 有用。这样可以过滤掉内容看似合理、实际没有帮助的经验。

#### 改进二：验证经验是否伤害其他任务

候选 Skill 不只在产生它的任务上测试，还要在以下任务中测试：

- 相似的新任务：检查它能否复用；
- 过去已经能完成的任务：检查它是否导致旧能力退化；
- 不同类型的任务：检查它是否会被错误使用；
- 安全和成本测试：检查它是否引入危险操作或过多 token、耗时和费用。

只有带来收益且没有明显副作用的 Skill 才能进入长期经验库。

#### 改进三：给 Skill 写清楚使用条件

每条 Skill 除了操作步骤，还必须包含：

- 适用于什么任务和环境；
- 依据哪些运行记录生成；
- 使用什么命令或规则验证结果；
- 在什么情况下不能使用或需要重新验证；
- 允许增加多少 token、耗时和费用。

检索到一条相似 Skill 后，系统还要检查当前任务是否满足这些条件。相似只表示“可能有用”，不表示“一定可以使用”。

#### 改进四：Skill 上线后仍然可以撤销

Skill 通过测试后会进入新的 Agent 策略版本。系统继续记录它在真实任务中的使用结果。如果工具、依赖、模型或任务环境发生变化，或者 Skill 的实际效果下降，系统会暂停使用并重新测试。确认有害时，可以废弃该 Skill 或回滚到之前的策略版本。

与 EDV 相比，我们的主要区别可以概括为：

| EDV | HammerLoom |
| --- | --- |
| 多个 Agent 投票判断经验是否可信 | 直接比较使用前后的任务结果 |
| 主要验证产生经验的任务 | 同时测试新任务、历史任务和不同类型任务 |
| 经验主要是自然语言内容 | Skill 包含适用条件、证据、验证方法和失效条件 |
| 验证通过后写入记忆 | 写入后继续监控，支持暂停、废弃和回滚 |
| 关注成功率 | 同时检查成功率、退化、安全、token、耗时和费用 |

## 2. 算法实现流程

### 第一步：记录任务轨迹

Agent 每次执行任务时，HammerLoom 记录：

- 用户任务；
- 模型调用；
- 工具调用及返回结果；
- 文件修改；
- 验证命令和验证结果；
- token、耗时和费用；
- Agent 版本、工具版本和环境信息。

没有明确验证结果的轨迹不能生成候选 Skill。

### 第二步：生成候选 Skill

系统比较同类任务的成功和失败轨迹，找出真正影响结果的步骤。例如：

- 是否先复现错误；
- 是否选择了正确工具；
- 工具参数是否正确；
- 修改是否集中在真正的错误位置；
- 失败后是否采取了有效的恢复动作。

然后将可复用部分整理为候选 Skill。候选至少包含：

```yaml
name: Skill 名称
scope: 适用的任务和项目
preconditions: 使用前必须满足的条件
steps: 操作步骤
evidence: 来源轨迹和关键事件
validators: 验证结果的方法
invalid_when: 需要停止使用或重新验证的条件
cost_limits: token、耗时和费用上限
status: candidate
```

如果候选内容无法指向具体轨迹证据，或者没有可执行的验证方法，则直接拒绝。

### 第三步：准备评测任务

系统为候选选择一组任务：

1. **来源任务**：确认原来的成功可以复现；
2. **相似任务**：确认 Skill 能用于其他同类问题；
3. **历史任务**：确认加入 Skill 后旧任务不会变差；
4. **不同类型任务**：确认 Skill 不会因为描述相似而被误用；
5. **安全和成本任务**：确认不会越权，也不会产生不可接受的开销。

每个任务都必须有确定的初始环境和验证方法。

### 第四步：成对运行评测

对每个评测任务创建两个相同的隔离环境：

```text
A 组：当前 Agent 策略，不加载候选 Skill
B 组：当前 Agent 策略，满足使用条件后加载候选 Skill
```

两组使用相同的模型、工具、初始文件、采样参数和资源上限。运行结束后保存以下结果：

- 任务是否完成；
- 验证器是否通过；
- 是否调用并遵循了 Skill；
- token、耗时和费用；
- 是否发生安全违规；
- 失败发生在哪一步。

如果模型输出具有随机性，同一对测试需要使用多个相同种子重复运行，避免把一次偶然成功当成 Skill 的效果。

### 第五步：决定是否晋升

系统根据真实评测结果作出决定：

- 来源任务无法复现：拒绝；
- 出现安全违规：拒绝；
- 导致重要历史任务失败：拒绝或人工检查；
- 对相似任务没有稳定帮助：继续观察或拒绝；
- token、耗时或费用超过上限：拒绝或人工检查；
- 带来稳定收益且没有明显退化：晋升。

候选可能处于以下状态：

```text
candidate -> testing -> active
                     -> manual_review
                     -> rejected
```

晋升时创建新的策略版本，并保存使用的评测任务、所有运行结果和晋升原因。

### 第六步：在后续任务中使用 Skill

新任务到来后，系统先按任务范围和内容召回可能相关的 Skill，再检查每条 Skill 的使用条件：

```text
召回 Skill
  -> 检查项目、任务类型、错误现象和环境版本
  -> 条件满足：注入 Agent Context
  -> 条件不满足：跳过并记录原因
```

系统记录 Skill 是否被召回、是否通过条件检查、是否真正被使用以及最终任务结果。这些记录用于判断 Skill 上线后的实际效果。

### 第七步：重新验证和回滚

出现以下情况时，Skill 暂停使用并重新测试：

- 模型、工具或依赖版本变化；
- 原有验证命令失效；
- 使用 Skill 后的成功率持续下降；
- 出现新的安全问题；
- 新 Skill 与旧 Skill 冲突。

重新测试通过后恢复使用；测试失败则废弃该 Skill。如果新策略版本造成整体退化，系统回滚到上一个有效版本。

最终流程如下：

```text
记录任务轨迹
  -> 生成候选 Skill
  -> 检查证据和验证方法
  -> 准备来源、相似、历史、不同类型及安全/成本任务
  -> 分别运行“不使用 Skill”和“使用 Skill”
  -> 比较成功率、退化、安全和成本
  -> 晋升、继续观察或拒绝
  -> 在新任务中按条件使用
  -> 持续记录效果
  -> 重新验证、废弃或回滚
```
